# V1.8.2-S5：Provider 真实 Usage 与诚实成本证据收敛

事实截止：2026-07-27（北京时间）

## 结论

**PARTIAL**

S5 的核心成本证据合同、Trace 展示、Flash/Pro 分账和真实 Provider Usage 已完成并部署。一次普通 Flash 聊天完整通过；唯一一次 Darwin Pro Provider 调用也已保存真实响应模型、三类 Usage、request_id、价格快照、公式和费用。该 Darwin HTTP 请求在证据落账后因响应组装引用旧变量而返回 500，Badcase 分析与草稿未落库。问题已确定性修复并重新部署，但遵守“一次 Darwin Pro 逻辑调用”上限，未再次调用模型，因此本阶段不把 Darwin 业务端点写成全链路 PASS。

旧 Key 的 2026-07-25 异常消耗仍是“来源未明的历史事实”，本阶段没有改写历史结论。

## Key 与运行基线

- 新 DeepSeek Key 已通过脱敏检查并进入 `demo-os-api` 容器；未记录或输出值、前后缀、长度、指纹。
- `.env` 未提交，NAS Git 状态仍只有 `.env` 未暂存。
- RuntimeRelease 保持 `rr_0019_8b4924eb / v19`，没有创建或发布新 RuntimeRelease。
- S5 前运行代码基线：`fad6318d72f66fbda1900236453f0739d326ea28`。
- S5 主实现 NAS Git 提交：`3d21321a77c3ef7f2433dd080cac5e36fd25653c`。
- Darwin 响应合同修复 NAS Git 提交：`80cc6b31dc86a2739cce729650284d73715cc6c8`。
- GitHub main 已使用用户本轮提供的临时 PAT 从 `979ea051e7480803635006a11894d76400cb4759` 安全快进到 NAS S5 提交；PAT 未写入 Git remote、仓库、`.env` 或持久脚本，remote 保持无凭据 HTTPS URL。

## 实现范围

- `app/settings.py`：使用 DeepSeek 响应证据适配器，捕获原始 `response.model`、request_id 和 Usage 扩展字段；`use_thinking` 与模型 ID 独立。
- `app/runtime/provider_evidence.py`：统一提取 Provider 响应模型与原始三类 Usage。
- `app/runtime/contracts.py`：补充 Provider 响应模型、Thinking、三类 Usage 与新来源枚举。
- `app/runtime/cost_ledger.py`：统一生成诚实成本条目；禁止从 total_tokens 拆分三类 Usage。
- `agents/router.py`、`app/runtime/coordinator.py`：Router 与垂直 Agent 分阶段记账。
- `app/badcases.py`：Badcase 分类使用 Flash，Darwin 使用 Pro，并按同一证据合同记账；修复 Darwin 返回旧变量问题。
- `app/observability.py`：Trace 详情提供模型身份、三类 Usage、价格、公式、单次/Trace/会话合计和按实际响应模型分桶。
- `frontend/index.html`：Trace 抽屉显示阶段、请求模型、Provider 响应模型、Thinking、Usage 来源、三类 Usage、费用证据和合计。
- `scripts/test_v182_s5_provider_cost_contract.py`：11 项无模型、无 HTTP、无数据库副作用的确定性合同检查。

没有新增数据库表或字段；新证据保存在现有 `model_calls.usage_normalized` JSON 和现有 Evidence Ledger 合同中。没有批量回填或改写历史记录。

## 模型身份与 Usage 合同

- `requested_model`：系统实际传给 Provider 的请求模型。
- `provider_response_model`：只读取 Provider 原始 `response.model`；Provider 未返回时保持空值，页面显示“未返回/未采集”。
- `thinking_enabled`：单独保存，Thinking 模式不等于 Pro。
- `prompt_cache_hit_tokens` → `input_cache_hit_tokens`。
- `prompt_cache_miss_tokens` → `input_cache_miss_tokens`。
- `completion_tokens` → `output_tokens`。
- Provider request_id 保存于现有 `usage_normalized` JSON。

来源判定：

- `provider_actual`：Provider 调用成功，且原始三类 Usage 全部存在。
- `estimated`：只有本地估算证据；不会冒充 Provider Usage，也不计算精确供应商费用。
- `unavailable`：Provider 失败、字段缺失、只有 total_tokens、或缺少实际响应模型/匹配价格快照。

历史 total-only 记录保持原貌，不反推、不平均拆分、不回填。

## 价格快照与公式

沿用 v19 当前有效快照（CNY / 1M Token，effective_date `2026-07-17`），本阶段没有修改价格：

- Flash：未命中输入 1.0，命中缓存输入 0.02，输出 2.0。
- Pro：未命中输入 3.0，命中缓存输入 0.025，输出 6.0。

公式：

`(输入未命中缓存 × 未命中输入单价 + 输入命中缓存 × 命中缓存单价 + 输出 × 输出单价) / 1,000,000`

只有三类 Provider Actual Usage、实际响应模型和对应价格快照同时存在时，才给出单次精确金额。

## 确定性测试

部署前后均通过；修复后为 11 项：

1. 三类 Provider Usage 原样提取和保存；
2. Flash/Pro 分别定价；
3. 请求模型与响应模型不一致时分别保存；
4. 响应模型缺失时不伪造；
5. total-only 金额不可得；
6. estimated 与 provider_actual 不混淆；
7. 历史记录兼容；
8. Provider 失败不保存成功成本；
9. Router/垂直 Agent 分阶段记账；
10. Darwin Pro 不混入 Flash；
11. Darwin 返回值读取统一 CostEntry。

前端内嵌 JavaScript `node --check` 通过。未运行旧回归脚本、Playwright、浏览器自动化或任何会批量写业务数据的测试。

## 真实 Flash 验收

- 请求数：1 次聊天请求；无重试。
- Trace：`4b4296811a274517`。
- 会话：`s5-flash-20260727-acceptance`。
- SSE：HTTP 200，终态 `complete`，Tool/MCP 调用均为 0。
- Router：Flash，Provider Actual；命中 896、未命中 1364、输出 121；费用 ¥0.00162392。
- 垂直 Agent：Flash，Provider Actual；命中 0、未命中 1619、输出 287；费用 ¥0.002193。
- Trace/会话合计：2 次 Flash 调用，4287 Token，¥0.00381692；Pro 0 次。
- Evidence Ledger：2 条 model_calls、2 条 cost_entries、0 action_proposals、0 action_receipts。

## 真实 Darwin Pro 验收

- Badcase：`#663`，调用前为 `classified`，关联真实失败 Trace `26f88f354b6f46dd`。
- Darwin 逻辑调用：1 次；无重试。
- 模型 Trace：`de931343e3d347bb`。
- Provider 模型调用：1 次 `deepseek-v4-pro`，Provider Actual。
- Usage：命中 0、未命中 2712、输出 2405，总 Token 5117。
- 费用：¥0.022566；Provider request_id 已保存，未在报告中输出。
- Trace 成本分桶：Pro 1 次、¥0.022566；Flash 0 次。
- HTTP 终态：500，原因是成本证据成功落账后，Darwin 响应仍引用旧局部变量。
- 修复：响应改为读取统一 CostEntry，并通过第 11 项确定性检查；只重建 API。
- 按模型调用上限未再次调用 Pro。Badcase `#663` 保持 `classified`，没有伪造分析、草稿或成功状态。

## 写操作与边界

- Flash 验收没有 ActionGateway、业务工单、人工接管或确认写入。
- Darwin 失败请求只新增了真实模型调用/成本证据；没有应用修复、改代码、发布配置或创建 RuntimeRelease。
- 没有重建 DB、删除数据、清理 Badcase/Trace/Release、重建 RAG 索引或创建企业级 FinOps/账单对账/密钥治理系统。

## T+1 与已知限制

**T+1供应商后台对账：待次日人工确认，不阻塞本次功能验收。**

- DeepSeek 后台非实时；本报告不把 T+1 写成已通过或失败。
- 应由用户次日对照新 Key 的 Flash/Pro 请求数、三类 Token 与金额。
- 历史 total-only 数据仍不可逐请求对账，不补造字段。
- Darwin 的 Provider 成本 Trace 可查询且证据完整，但本次 Badcase 分析/草稿未落库，因此整体结论为 PARTIAL。
- GitHub main 与 NAS 已同步；`.env` 仍是唯一未提交文件。

## 部署与回滚

- 首次部署只重建 `demo-os-api` 和 `demo-os-web`；DB 未重建。
- Darwin 合同修复只重建 `demo-os-api`；Web/DB 未重建。
- RuntimeRelease 始终为 v19。
- 代码回滚基线：`fad6318d72f66fbda1900236453f0739d326ea28`。
- S5 主实现回滚点：`3d21321a77c3ef7f2433dd080cac5e36fd25653c`。
- Darwin 合同修复回滚点：`80cc6b31dc86a2739cce729650284d73715cc6c8`。
