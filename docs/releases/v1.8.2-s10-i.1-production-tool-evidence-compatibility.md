# V1.8.2 S10-I.1｜生产 ToolEvidence 兼容收口

日期：2026-08-07
类型：面试演示版纯代码修复；不发布 RuntimeRelease

## 修改前

- Agno MCP 的生产返回 `{"result":"<业务 JSON>"}` 被保存成单个 `$.result` 字符串，天气和工单字段没有形成独立 Facts。
- Citation Renderer 只把 RAG `evidence_id` 视为合法 marker；当前 Bundle 内真实存在的 `tool_ev_*` 会被误判为未知证据。

## 修改后

- 只解包根级、单一 `result` 且字符串可解析为 JSON object/list 的传输包装；多键业务对象、嵌套 `result`、JSON scalar 和 calculator `{"result":84}` 保持原语义。
- 合法证据 ID 由本轮 RAG 候选与本轮 ToolEvidence 共同组成。Tool marker 必须由同一 ToolEvidence 的结构化 Facts 支持并形成 ToolEvidenceLink，但不生成 RAG 引用编号；未知 `tool_ev_*` 继续 fail-closed。
- 29→30、70→71、工单状态改写仍由现有结构化事实校验拒绝。

## 验证边界

- 断网、临时数据目录、空 Provider Key；不连接生产数据库，不调用模型。
- 覆盖生产 `structured_content.result` 形态、天气真实字段、工单 `data` 数组、RAG＋两条 Tool marker 联合交付、错误数值/状态与未知 marker 反例，以及 calculator 84 回归。
- Router、Agent、RAG 检索、Top-K、阈值、Skill/MCP 配置、Badcase、Evaluation、Trace 成本、数据库和 RuntimeRelease 均未改变。

## 回滚

使用本提交的普通 `git revert <commit>`，随后只重建 API；数据库、业务 Volume、历史 Trace 与 RuntimeRelease v27 均无需回滚。
