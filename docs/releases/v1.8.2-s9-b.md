# V1.8.2-S9-B 发布记录：成本口径修正与一屏式 Trace 成本解释

事实截止：2026-07-28（北京时间）

## 结论

**PASS**。今日聚合已限定为北京时间当日 00:00 至当前时间；Provider 真实成本、本地估算和金额不可计算严格分桶。Trace 详情已收敛为“人话结论、调用链、成本口径、唯一优化建议、复测方法”，无模型流程明确显示 Token 与费用不适用。

## 实现与发布边界

- 代码基线：`b31ea4ec566be7be5cb9f77abf5f260afb2cf2a2`
- S9-B 代码提交：`2ee4f90`
- `app/observability.py`：今日范围、严格成本分桶、Provider 真实成本排行、Trace 成本解释和单一建议合同。
- `frontend/index.html`：一屏式 Trace 成本说明、高成本 Trace 入口、无模型语义和只读复测方法。
- `scripts/test_v182_s9b_cost_story.py`：8 组无模型确定性合同检查。
- 没有新增数据库表或字段，没有修改价格、模型路由、Agent/Skill/RAG/MCP、Badcase 或 Evaluation。
- 只重建 `demo-os-api` 与 `demo-os-web`；数据库和 RAG 未重建；RuntimeRelease 保持 `rr_0026_a27a63e8 / v26`。
- 本阶段模型调用数为 0；未运行 A/B、Flash、Pro 或 Darwin。

## 四项口径修正

1. “今日成本”由全历史混合口径改为北京时间今日 00:00 至当前时间，并显示范围与时区。
2. `provider_actual`、`estimated`、`unavailable` 分开统计；估算注明非供应商账单，缺失金额不按 0 元处理。
3. 无 `model_call` 的规则 Trace 显示“本轮未调用模型，因此模型Token与费用不适用”。
4. 真实成本排行仅包含当前范围内全部调用均为 `provider_actual` 的 Trace，最多 5 条；估算和不可计算记录不替代。

部署后今日聚合为 2 次模型调用、Provider 真实成本 ¥0.00301020、本地估算 0 次、金额不可计算 0 次；三类已知 Usage 为命中缓存 2,560、未命中缓存 2,249、输出 355。历史累计仍单独折叠展示，不与今日混淆。

## 既有 Trace 验收

| Trace | 结果 |
|---|---|
| `3c1aad38350c4600` | 2 次 Flash（Router＋垂直 Agent），5,164 Token，Provider 真实成本 ¥0.00301020；建议优先降低 Router 缓存未命中输入。 |
| `7a9ef83684304e95` | 1 次 Darwin Pro，4,670 Token，Provider 真实成本 ¥0.01188720；Thinking 独立展示，建议收紧输出结构与长度。 |
| `1d33138475aa4a80` | 0 次模型调用；Token 与费用显示不适用，不进入不可计算桶。 |
| `61fa63d62f5847dd` | 历史失败调用缺少实际响应模型、三类 Usage 和金额；显示“金额不可计算”，不显示 ¥0，不进入真实成本排行。 |

## 测试与健康

- S9-B 确定性合同：8 组通过。
- S5 Provider 成本合同回归：11 项通过。
- Python 编译通过；前端 2 个内联脚本块解析通过。
- `/api/agents`、`/api/knowledge/docs`、`/api/observability/overview`、`/api/runtime/releases/current` 和 Web 首页均为 HTTP 200。
- `demo-os-api` healthy；`demo-os-web`、`demo-os-db` 运行正常。

## 已知限制

历史 total-only 或缺失 Provider Usage 的调用仍只能显示“金额不可计算”；本阶段按合同保留原貌，不批量回填或用供应商日账单反推单条 Trace。

页面最终视觉效果由产品负责人手动点验；本阶段未使用浏览器自动化。
