# V1.8.2-S10-B.2-Fix2.2：Lane、任务类型与回答合同正交化

## 结论

`PARTIAL`。Fix2.2 正交合同、无模型测试、API 部署与 N241 保存响应离线重放均通过；断点评估从 N242 继续，N242—N254 一次通过，N255 首次出现新的严格 Schema 失败后按合同立即停止，未重试，也未进入 T1—T5。

## 产品合同

- `lane` 表达领域与权限边界，`request_kind` 表达用户任务类型，`response_mode` 表达最终执行方式，三者互不冒充。
- C Lane 合法支持 `fact` 与 `general`，两者均生成 `safe_general`；C 不启用物业 Skill、RAG、MCP/Tool 或 ActionGateway，也不得冒充实时、官方或物业依据。
- B + fact 才生成 `grounded_answer` 并要求当次合法 Evidence；A + emergency、B 的四类任务、C 的三类任务及 CLARIFY + ambiguous 均由确定性映射生成 AnswerContract。
- 跨 Lane、Domain、目标 Agent、澄清字段或非法枚举的矛盾组合仍由严格 Schema 拒绝。

## 实现与无模型验证

- 修改 `LaneDecision` 合法矩阵与确定性 `AnswerContract` 映射；Router 语义 Prompt 未修改。
- 增加 N241 保存响应离线重放器，以及校验 N201—N241 不可变断点后仅从 N242 开始的续跑能力。
- Fix2.2 主合同 46 项通过，Provider 调用 0；S3、S5、S7-Fix2、S8-C、S9-B.2、S9-C 关键合同回归通过。
- 未在生产代码加入业务关键词、正则词表、题目专用分支或默认 Lane 兜底。

## N241 零模型重放

- 原 Trace：`ca0b933962854cf9`；原 Provider 响应与 request_id 保持不变。
- 结果：Lane=`C_ISOLATED_GENERAL`，request_kind=`fact`，response_mode=`safe_general`，Schema 有效。
- 物业 Skill、RAG、MCP、Tool、ActionGateway 均为 skipped；离线重放新增 Provider 请求 0 次，未伪造 request_id 或 Usage。
- 证据：`/volume3/docker/agno-demo-os-backups/s10b2-fix2-n241-offline-replay-20260730.json`，SHA256 `8ae966e9d2c15809a16217b8a490c3ecef8e29a03242ddecd189d01ecfd1dd16`。

## N242—N300 断点评估

- N201—N240 未重跑；N241 未再次调用；续跑从 N242 开始。
- N242—N254 一次通过；N255 首次失败后立即停止。续跑实际新增 Flash Router 请求 14 次，Pro / Darwin 为 0。
- 续跑 Provider Usage：缓存命中输入 3,584、缓存未命中输入 33,058、输出 7,239，总 Token 43,881，费用 ¥0.04760768。
- 截止 N255 的合并结果：A 18/18、B 16/16、CLARIFY 1/1、C 19/20；Lane 54/55，Schema 54/55。
- N255 Trace：`1fef67eed7bb4f10`。Provider 返回 C/general，但 `target_agent_id=null`，违反既有“业务 Lane 必须选择同域 Agent”跨字段 Schema；这是新的严格 Schema 失败，不是 C + fact 合同失败。
- 结果证据：`/volume3/docker/agno-demo-os-backups/s10b2-fix2-router-eval-fix2.2-20260730.json`，SHA256 `9983509722d641a03a48cc56ea13246f8c6bfcaa390f3773d7b15143f26061d7`。

## T1—T5

未执行。完整 100 题没有通过，按停止规则不得进入端到端验收，因此没有额外模型调用。

## Git、部署与回滚

- 运行代码提交：`f7d5186`；仅 API 因代码变化重建。
- RuntimeRelease 保持 `rr_0027_3ec523af / v27`，未发布 v28；Web、DB、RAG 均未重建。
- Fix2.2 修改前回滚基线：`66a2ad91ae1d592075251bcae4ae60a66a909898`。
- NAS 备份：`/volume3/docker/agno-demo-os-backups/s10b2-fix2.2-pre-66a2ad9-20260730.tar.gz`，SHA256 `6959c9790f150810600665c7f044eb19f1bc9cbf44ed9fb28ce6f699ec534f1b`。
- 回滚可普通 `git revert` Fix2.2 运行代码提交后仅重建 API，或恢复上述备份后仅重建 API；不得改写历史 Trace、数据库、RuntimeRelease 或 `.env`。
