# V1.8.2 Target① A 路人工协同合同

- 日期：2026-08-07
- RuntimeRelease：保持 `rr_0027_3ec523af / v27`
- 范围：只修 A 路，不改 B/C、能力配置、质量治理或成本链

## 修改前 → 修改后

- 修改前：明确转人工可表现为 `B/C + user_requested_handoff`；Handoff 已创建但 Lane SSE、Ledger 和 Trace 仍显示原 B/C；所有 A 又共用紧急安全文案。
- 修改后：明确转人工在正式落账前形成有效 A，并以 `user_requested/property_service/safety_override=false` 执行普通人工协同；现实安全风险以 `safety_risk/emergency/safety_override=true` 执行紧急人工协同。

## 证据合同

- Router 合同直接要求明确转人工输出 A；
- 运行时结构化不变量在 lane SSE、AnswerContract、Ledger 和 Trace 之前生效；
- A 路统一短路 Agent Selector、垂直 Agent、Skill、RAG、MCP/Tool、Draft/Proposal 和 ActionGateway/Receipt；
- 前端 Trace 主层统一显示“A路：人工协同”，并展示普通/紧急子类型；
- 不迁移数据库，不改历史 Trace，不发布新 RuntimeRelease。

## 验收与回滚

断网临时数据库行为测试覆盖普通转人工、安全转人工、普通 C 反例、否定/咨询规则和全能力短路；部署后仅允许一次真实明确转人工 SSE。回滚使用普通 `git revert`，随后仅重建 API/Web。
