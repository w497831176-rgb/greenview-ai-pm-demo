# V1.8.2-S10-H quality-closure truthfulness

Date: 2026-08-06

## Scope

- Close two quality-loop P1 defects without adding a framework or changing the product boundary.
- Keep ordinary owner chat, controlled writes, the optional-background budget gate, Trace accounting, and RuntimeRelease configuration unchanged.
- Validate only with isolated SQLite data, deterministic runtime stubs, a complete dependency image, and no Provider credentials or outbound network.

## P1-1: Evaluation response truthfulness

- The successful Golden Case response now returns the actual `budget_gate` result instead of referencing an undefined `budget` name after the run has already been persisted.
- The budget check remains before runtime execution. Evaluation Run creation, Trace linkage, failed-run persistence, and Evaluation-to-Badcase linkage keep their existing semantics.
- A successful HTTP run is proved to create exactly one Evaluation Run and zero Provider attempts when the chat boundary is replaced by a deterministic local result.
- A runtime failure remains a failed Evaluation Run with a failing completion rule and a linked Evaluation Badcase; it never becomes PASS.

## P1-2: Badcase retest truthfulness

- The Badcase retest consumer now parses the canonical SSE `event` and `data` fields instead of treating every data payload as success.
- `event:error`, a non-complete terminal event, multiple terminal events, a missing terminal event, a missing answer, or a missing Trace is a controlled retest failure.
- A successful retest accepts only one `done(status=complete)`, a non-empty final answer, and a persisted complete Trace belonging to the same retest session.
- Failure returns HTTP 502, records only non-sensitive failure evidence, and does not change lifecycle state or successful retest fields. No synthetic `retest-*` Trace ID is created.
- The successful context records `run_status=complete`, the retest start time, persisted Trace status, and Trace/session binding. A changed status or apply anchor during the run invalidates the result.
- `verify-pass` requires a post-apply start and completion time, non-empty answer, matching Trace/context evidence, and a live complete Trace row for the same session.
- Linked Evaluation retests use the same execution-complete Trace contract while preserving the original Evaluation result separately as `evaluation_run_status`.

## Data and runtime boundary

- No database migration, production-data mutation, historical Badcase rewrite, or Trace backfill.
- Historical retests that do not contain the new complete evidence contract remain retained but do not unlock a new verify-pass; the operator must perform a new truthful retest.
- No RuntimeRelease is created. The active capability release remains `rr_0027_3ec523af / v27`.
- No real model or Provider request is used for implementation or acceptance.

## Deterministic evidence

- `scripts/test_v182_s10h_quality_closure_truthfulness.py`: real FastAPI HTTP behavior over an isolated SQLite database, deterministic SSE/runtime stubs, Evaluation success/failure persistence, Badcase success/failure terminal semantics, Trace/session binding, post-apply gating, and zero Provider attempts.
- `scripts/test_v160_quality_governance_contract.py`: pure lifecycle gate fixture updated to the complete retest evidence contract.
- Existing Golden Set, Badcase workbench, three-lane runtime, controlled-write, background-budget, Provider reconciliation, cost, Python compile, and frontend syntax checks remain regression gates.

## Acceptance record

- Full-dependency isolated behavior suite: **PASS** — 94 HTTP/database/SSE behavior checks, zero Provider-attempt delta.
- Budget and Trace fail-closed regression: **PASS** — 301 checks with `DEFERRED_CHECKS=0`.
- Provider reconciliation and price-snapshot regressions: **PASS** — 79 and 11 deterministic checks; Provider calls remain zero.
- Quality lifecycle, Golden Set, and Badcase workbench regressions: **PASS** — V1.6 lifecycle contract, S6 (14 checks), and S7 (44 checks).
- Ordinary-chat/runtime contract: **PASS** — S10-B.2-Final deterministic runtime contract.
- Controlled-write behavior: **PASS** — eight direct ActionGateway, Receipt, cancellation, confirmation, and idempotency behaviors from the V1.8 runtime suite.
- Frontend syntax: **PASS** — two script blocks parsed. Python compile checks for every changed Python file also pass.
- The legacy V1.8 aggregate currently stops at an unrelated, outdated citation-rendering expectation, and the old S4A2 frontend check stops at an outdated source-string expectation. Neither is presented as S10-H behavior evidence; the required runtime and controlled-write paths were executed directly instead.
- Provider requests generated by this acceptance: **0**.
- Production service and fixed-history results are recorded only by post-deployment read-only verification.

## S10-H.1: Evaluation terminal-evidence closure

- Evaluation no longer synthesizes `complete`, accepts `message_id` as a terminal event, or lets `final` substitute for the canonical SSE `done` event.
- Rule scoring starts only after exactly one parseable `done(status=complete)`, no `error` event, a non-empty answer, and a non-empty real `trace_id` have all been observed.
- The referenced Trace must already exist, have `status=complete`, and belong to the exact Evaluation session. Missing, failed, or cross-session Trace evidence is a controlled failed Run.
- Malformed or non-object `error`/`done` payloads, duplicate done events, unknown/non-complete status, missing terminal evidence, a stream interruption, and any business event after done are deterministic failures.
- A failed execution still creates exactly one failed Evaluation Run, retains any real Trace evidence obtained before failure, follows the existing Evaluation-to-Badcase linkage, and never opens Badcase `verify-pass`.
- The success and 22 failure variants use the real FastAPI route and real `_run_real_chat`; only the lower SSE source is deterministic. The complete S10-H suite passes 314 behavior checks with zero Provider-attempt delta in a `--network none` full-dependency API container.
- No schema, frontend, ordinary chat, runtime capability configuration, historical ledger, or RuntimeRelease changes are included. Active RuntimeRelease remains `rr_0027_3ec523af / v27`.

## Rollback

Use a normal `git revert` of the S10-H commit and rebuild only `demo-os-api`. Do not reset, delete data, rebuild the database, or alter volumes.
