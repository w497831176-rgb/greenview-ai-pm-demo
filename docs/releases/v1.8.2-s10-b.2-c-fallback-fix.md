# V1.8.2-S10-B.2-C-Fallback-Fix

结论：**PASS**。Handoff 保持 PASS；C 类无同域 Agent 的确定性回退已恢复正常交付。

## 根因与修复

- 两条失败 Trace 的 Router 均正确判为 `C_ISOLATED_GENERAL`，且均未请求 Handoff。
- Agent 选择无同域命中后，C 类回退把 Write 与 Handoff 状态写成通用的 `skipped`。
- `CapabilityDecision` 的既有合同只允许 Write 为 `required/not_required`，Handoff 为 `available/required/not_required`，因此在生成最终回答前校验失败。
- 代码提交 `66678e9` 统一由一个构造器生成无 Agent 回退状态：C 类 Write/Handoff 均为 `not_required`；Skill/RAG/Tool 仍为 `skipped`。未修改 Router Prompt、Handoff 规则或 RuntimeRelease。

## 无模型验证

- 新增 C 类回退确定性合同，覆盖通用 Agent 命中、无 Agent 边界、安全拒绝、Trace/Evidence 完整终态和内部错误不外泄。
- 既有 Handoff 合同与三分类运行时合同回归通过。
- 未使用关键词、正则、白名单或验收句分支。

## 历史证据离线重放

| 原失败 Trace | 派生重放 Trace | Provider 调用 | Trace / Evidence |
|---|---|---:|---|
| `50611a73ec7a41aa` | `4e27f460bde4733f` | 0 | complete / complete |
| `edcfdc8e5dd04507` | `d1e007910d4b17a9` | 0 | complete / complete |

原 Trace、原 Router 结果和原模型调用均未改写；两次重放均保持 Handoff 0、物业能力 0。

## 三条唯一真实验收

| 输入 | Trace | 结果 | Handoff | Provider |
|---|---|---|---|---|
| 暂时不用转人工，只想了解一下。 | `c0675b5277dc479b` | C 类 complete，自然能力边界 | 0 | Flash 2 次，1,667 Token，¥0.00146124 |
| 转人工需要什么条件？ | `9b079ed0a5dc41e1` | C 类 complete，未进入知识不足拒答 | 0 | Flash 2 次，1,808 Token，¥0.00174724 |
| 那转一下人工。 | `2ffb3e31a5db444a` | complete，等待工作人员领取 | requested / user_requested | Flash 1 次，1,027 Token，¥0.00086124 |

合计 Flash Provider 请求 5 次：缓存命中 1,536、缓存未命中 1,893、输出 1,073，总 Token 4,502，Provider 真实成本 ¥0.00406972。Pro、Darwin、物业 RAG、Tool、ActionGateway 均为 0，未新增 Badcase。

## Badcase 与部署

- Badcase #680 保留原始失败及重复出现证据，关联离线重放、代码修复和真实复测后按正常生命周期关闭。
- 仅重建 `demo-os-api`；`demo-os-web` 与 `demo-os-db` 未重建。
- RuntimeRelease 保持 `rr_0027_3ec523af / v27`。
- 回滚基线：`4cfab137962f1702de07c0ee6a7d30b2efd3b627`；需要回滚时 revert 本修复代码提交并仅重建 API。
