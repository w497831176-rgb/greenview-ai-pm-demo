# V1.8.1｜目标 1、2 业务层精简发布记录

> 状态：`RC1_DEPLOYED / BACKEND_SMOKE_VERIFIED / UI_PENDING`
> 基线：annotated tag `v1.8.0`
> 基线 commit：`867948a50789ce1fcc8250b42a54086ebdefffa8`
> 正式版本条件：发布一、发布二均完成自测、自修、部署，并由用户联合手点验收

## 发布链

```text
v1.8.0
→ v1.8.1-rc.1
→ v1.8.1-rc.2
→ v1.8.1
```

## 发布一｜v1.8.1-rc.1

范围：

- 退役图片上传、图片分析上下文、Kimi Vision API 和相关环境变量；
- 历史多模态表和资产非破坏性保留；
- 员工端待派单、处理中、已完成合并到一个“工单管理”状态页；
- 人工接管保持独立入口；
- Agent、Skill、MCP、知识库四个管理菜单保持独立；
- 删除重复活文档，归档明确过时的 V1.2 业务说明；
- 不改目标 3、目标 4 或 Agno 底层能力。

验证门槛：

- Python 编译检查；
- 前端 JavaScript 语法检查；
- 33 条 V1.8 无模型合同；
- `/api/multimodal/*` 不再提供；
- 目标 1 只读综合咨询和受控写边界定向后端冒烟；
- 目标 2 CRUD、绑定、Compile、Publish、下一新会话定向后端冒烟；
- `/api/agents`、`/api/knowledge/docs`、
  `/api/observability/overview` 均返回 200。

当前验证结果：

- 精确 rc.1 worktree Python 编译通过；
- 33 条 V1.8 无模型合同全部通过；
- 前端 JavaScript 语法检查通过；
- FastAPI 应用导入和 247 条路由装配通过；
- `/api/multimodal/*` 不在路由表，部署后请求返回 404；
- 实现 commit：`e6d113e`；
- main merge commit：`3fade281f75770df8c125b62942334167fc6919e`；
- NAS 运行 SHA 与 main merge commit 一致；
- `demo-os-api` 镜像：
  `sha256:982abc4fddd2890abc2d8f171f89e84d41a440208e23e895228140f108b885a0`；
- `demo-os-web` 镜像：
  `sha256:84e8b24b5d8b38a36aa9840ab35eb3c796ac289782d350e85b9e42a368e883d35`；
- `/api/agents`、`/api/knowledge/docs`、
  `/api/observability/overview` 均返回 200；
- 真实只读综合咨询 Trace：`c643be9cc92b4be6`；
- 只读 Trace Acceptance：
  `accept_693bdeb51ae240afbe8b01c7127bf380`，10 项断言全部通过；
- 真实命中 maintenance Router/Agent、Skill 8、文档 1/4 的 3 个 Citation、
  3 个 read MCP、Evaluation、2 个 Cost stage，且零业务写入；
- 受控工单草稿 Trace：`b3b4522c55964598`，状态 `paused/draft_updated`；
- 自然语言取消 Trace：`fbecbb2ba2f94e75`，状态
  `completed/rejected`；
- 工单总数 62 → 62，ActionReceipt 0，MCP 写入 0；
- Agent、Skill、MCP、知识库、Current Release、新会话 Snapshot 六个管理/
  运行接口均返回 200；
- 当前配置 Compile 结果 `valid=true`、`errors=[]`，测试 Draft Release 为
  `rr_0013_6f887af5`，未发布、未改变当前 Release；
- 当前 Published RuntimeRelease 仍为 `rr_0012_586e0fa1`，配置 Hash
  `18564ef030e8a744c9154b1b6f0ecca7c4eacfd51e0e19ff2efa81cfa2ebd9a78`；
- `.env` 原样保留为唯一 Git dirty；`check_db2.py` 仍存在且未 stage。

当前结论：`BACKEND_SMOKE_VERIFIED / DEPLOYED / UI_PENDING`。发布一可以进入
`v1.8.1-rc.1` 基线，但不得表述为用户 UI 已验收。

## 发布二｜v1.8.1-rc.2

范围：

- 移除 `RUNTIME_ENGINE=v17|v18` 的双业务运行时开关；
- 从 Legacy Adapter 中保留纯 API/SSE 适配，移除 V1.7 业务执行路径；
- 退出不再参与 V1.8 运行时的旧静态业务 Agent 工厂和默认业务 Tool；
- 保留 Router、当前工单受控写链、RuntimeCoordinator；
- 保留 Agno、AgentOS、AgentFactory、WorkflowFactory、Skills、MCP、HITL、
  Tracing、RuntimeRelease、Snapshot、Evidence/Cost Ledger；
- 不改目标 3、目标 4。

发布二只能在 rc.1 部署冒烟通过后开始；该条件现已满足。

实现状态：`IMPLEMENTED / CONTRACT_VERIFIED / DEPLOY_PENDING`。

- `legacy_chat.py` 已收敛为纯 HTTP/SSE、历史/反馈与人工接管协议层；
- 业主聊天始终直接委派 `RuntimeCoordinator`，不存在环境变量切换旧业务执行器；
- 删除四个旧静态业务 Agent 工厂，平台 Agent 继续由 Snapshot + AgentFactory 构建；
- 历史断言辅助函数已隔离，生产运行时不导入；
- Python 编译通过，34 条 V1.8 无模型合同全部通过；
- 部署后的真实 SSE、受控草稿/取消、动态配置 Compile/Snapshot 证据待补。

## 回滚

- rc.2 失败：优先回滚到 `v1.8.1-rc.1`；
- 整个 V1.8.1 精简方向失败：回滚到 `v1.8.0`；
- 不执行 force push、`git reset --hard`、`git clean` 或数据卷删除；
- `.env`、`check_db2.py` 和业务数据保持原样。

## 发布证据

完成每个 RC 后补充：

```text
Git SHA
annotated tag
NAS 运行 SHA
容器镜像 ID
合同测试结果
定向冒烟 Trace/Acceptance ID
三个健康接口状态
dirty 文件保留状态
```
