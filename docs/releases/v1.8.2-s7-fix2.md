# V1.8.2-S7-Fix2 — 日历 Tool 选择合同与 #677 最终关闭

状态：PASS

## 目标与边界

本轮只修复 `calendar-server/get_current_datetime` 因“预约”语义产生的误触发，
并对 Evaluation Case #2 执行一次最终 Flash 复测。未调用 Darwin/Pro，未运行
其他黄金用例、A/B、Canary 或普通聊天；未修改 Agent #74、RAG、安全拒答、
Handoff、ActionGateway、目标①②或 S5 成本底座。

## 根因证据

- Case #2 完整问题含“专用预约号码”。
- Run #6 / Trace `14bdba02ae894b7d` 的 ToolInvocation 明确记录：
  `planner_source=published_tool_metadata`、`match_reason=触发词=预约`。
- v25 快照中 `get_current_datetime.trigger_keywords` 为：
  `今天、明天、日期、星期、几点、预约`；Tool 描述只是“获取当前北京时间日期和时间”。
- 实时 Draft 的原始 Tool 元数据只有 discovery 信息；过宽关键词来自内建兼容元数据，
  并在 v25 编译后成为不可变发布配置。
- 调用顺序为 Flash Router → 确定性 `plan_tools()` → read MCP 预调用 →
  垂直 Agent。Run #6 的垂直模型因无证据门禁被跳过，因此不存在模型生成日历
  ToolPlan 的可能。
- 现有 `/api/mcp-servers/{server}/tools/{tool}/runtime-policy` 可以保存
  operator-declared 覆盖，所以无需修改 Tool Planner 代码。

## 配置修复与发布

通过现有管理 API 只更新 Tool #108：

- 修改前：`今天、明天、日期、星期、几点、预约`；
- 修改后：`今天几号、今天是几号、今天星期几、今天是星期几、现在几点、
  现在是几点、当前日期、当前时间、日期计算`；
- `effect=read`、`risk_level=L1`、`execution_mode=auto_preinvoke`、
  Tool、Server、Agent 绑定和结果合同均不变。

平台 Draft 中仍存在两项旧契约测试 Agent 和一项客服 Skill 历史漂移，因此没有
直接发布整个 Draft。新 Release 从 v25 不可变快照派生，并只替换目标 Tool 元数据：

- Release：`rr_0026_a27a63e8 / v26`；
- Parent：`rr_0025_013cbdee / v25`；
- Config Hash：`60fe65fdb86f2e1dca6295ccceea8616485a958b852f6e8a00bccffdf07c87c3`；
- 校验：valid，10 Agents、11 Skills、34 Knowledge Docs、6 MCP Servers、
  24 Tool Policies；
- 深比较：除 `calendar-server/get_current_datetime.tool_metadata` 外，v25 与
  v26 配置完全相同；
- 新会话使用 v26，旧会话继续固定原 Snapshot；无需重建容器。

## 无模型确定性验证

`scripts/test_v182_s7_fix2_calendar_contract.py` 共 14 项 PASS：

1. “今天几号”“现在几点”“今天星期几”命中 `get_current_datetime`；
2. “如何预约物业服务”“有没有免费搬家服务”、Case #2 原文和“预约维修”均跳过日历；
3. “今天几号，我想预约维修”允许命中，原因来自“今天几号”，不是“预约”；
4. 精确工单号查询仍命中 workorder 只读 Tool；
5. 无证据安全拒答门禁仍为 `rejected_insufficient`；
6. 火星温室领域仍路由 Agent #74，Case #2 仍路由客服；
7. Tool skipped 的 `not_required` 原因仍进入 Trace/Evidence 决策摘要。

S7 原有工作台回归脚本继续为 43 项 PASS。全部测试使用内存/临时数据和纯函数，
未调用模型、MCP 或 NAS 业务数据库。

## 唯一一次真实复测

- Evaluation Case：#2 / `S6-GOLDEN-002`；
- Run：#7，passed；
- Trace：`973ad764aae34d36`，complete；
- Session：`evaluation-S6-GOLDEN-002-8551996a`；
- Snapshot：`rr_0026_a27a63e8 / v26`；
- Agent：`customer_service`，未命中 `mars-greenhouse-agent`；
- Tool：skipped，reason=`not_required`；MCP 0；
- RAG：`rejected_insufficient`；Citation 0；
- 回答明确说明“当前知识依据不足”，未编造服务、号码、费用或办理步骤；
- 回答中的“或选择转人工核实”仅是用户可选建议，真实 Handoff 为 0，未声称已经转接；
- ActionGateway、工单、草稿、Proposal、Receipt、业务写入均为 0。

模型与成本证据：

- model_call：#959，仅 Router 1 次；
- requested/provider model：均为 `deepseek-v4-flash`；
- usage_source：`provider_actual`；
- 输入命中缓存 0、输入未命中缓存 2,265、输出 134、总 Token 2,399、
  reasoning 97；
- 价格快照：Flash 未命中输入 ¥1/1M、命中输入 ¥0.02/1M、输出 ¥2/1M；
- 费用：`(2265×1 + 0×0.02 + 134×2) / 1,000,000 = ¥0.002533`。

## Badcase 与 Evaluation 闭环

#677 保留两层根因：

1. 原始 Run #2 / Trace `1098e350a28e4e11`：Agent #74 路由语义过宽，
   误选火星温室 Agent；由 v25 修复。
2. 首次复测 Run #6 / Trace `14bdba02ae894b7d`：路由已正确，但“预约”
   误触发日历 Tool；由 v26 修复。
3. 最终 Run #7 / Trace `973ad764aae34d36`：全部硬断言通过。

Darwin Trace `6ed1a63a8fc446a9` 和被人工拒绝的草稿 #18 继续保留；本轮没有
再次调用或应用 Darwin。#677 已完成 `fixing → verifying → released → closed`。

评估口径：累计 5/7 PASS（保留两次真实失败），每个黄金用例最新结果 5/5 PASS，
100%。本轮只复测失败用例 Case #2，没有重跑其余四题。#678 仍为 pending，未修改。

## 工作台与部署回归

- Badcase 默认 20 条分页连续三次为 33.07 ms、18.59 ms、18.38 ms；
- 响应体 23,439 bytes，保持约 23 KB，NAS 内部低于 1 秒；
- #677 详情 HTTP 200，可展示两阶段根因、v25/v26、Run #2/#6/#7 和关闭时间线；
- 技术 JSON、Prompt 与动作原文仍默认折叠；
- #678 HTTP 200，仍为 pending；
- 未修改 Web，无浏览器自动化，最终视觉继续由用户手点；
- 未重建 API、Web、DB 或 RAG，未修改或提交 `.env`。

