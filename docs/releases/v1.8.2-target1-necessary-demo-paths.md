# V1.8.2 Target 1｜三分类必要演示路径收口

## 范围

仅收口 A/B/C 三分类上的四条必要演示路径，不实施固定能力目录、质量治理或成本治理改造。RuntimeRelease 继续使用 `rr_0027_3ec523af / v27`。

## 修复

- B-Tool：成功只读 Tool 已经是本轮合法业务证据时，不再因为无关 RAG 候选存在而强制模型补 RAG 引用；纯 RAG 回答仍必须通过引用门禁。
- Trace 主层：`selected` 只显示“已选择”，不再冒充“已执行”；最终 Tool 依据只统计调用成功且业务成功的结果，Receipt 只统计带真实资源 ID 的 committed 回执。
- 未改变 Router、Agent/Skill/RAG/MCP 配置、检索参数、ActionGateway、数据库结构、Provider 账本、时间口径或价格计算。

## 确定性验收

- 既有 A/C、Handoff、RAG 专项继续使用。
- 新增 `scripts/test_v182_target1_necessary_demo_paths.py`：用临时 SQLite 和断网环境验证 B-RAG、B-Tool、Draft→Proposal→确认→Receipt 幂等链路，Provider Attempt 为 0。
- 生产真实 SSE 仅按独立简单场景各运行一次；受控写只运行到 Proposal/paused，业主确认留给用户手点。

## 回滚

对本提交执行普通 `git revert <commit>`；仅重建受影响的 API/Web，不迁移或重建数据库，不修改 RuntimeRelease。
