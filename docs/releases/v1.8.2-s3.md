# V1.8.2-S3：RAG Evidence 质量收敛

状态：Git main 已固化；NAS 活部署待工作区收敛授权  
复杂度：M  
范围：切片、检索 Evidence 门槛、无证据回答、固定评估集

## 用户可见目标

- 标题不再作为可点击的独立证据；
- 正确证据能进入最终 Hybrid Top-K；
- 弱证据和无证据问题不再被模型补成确定性物业规则；
- 时间、金额和数量必须被对应引用直接支持。

## 固定基线

- 47 个知识文档、564 个 Chunk；
- 标题/结构型 Chunk 186 个，空白 Chunk 0 个，过短 Chunk 22 个；
- 8 道有答案题：Keyword Hit@5 6/8、Vector Hit@5 5/8、
  Hybrid Hit@5 7/8、Hybrid Top-3 6/8；
- 4 道无答案题：拒答 0/4；
- 已知两条真实 Trace 的核心结论引用正确 1/2。

## 通过门槛

- Hybrid Top-5 至少 7/8；
- Hybrid Top-3 至少 6/8；
- 无答案拒答 4/4；
- 演示回答中的数字和时效引用正确率 100%；
- 标题型 Chunk 不进入最终 Evidence。

## 明确不做

不更换向量库，不引入 GraphRAG/Cross-Encoder，不建立通用 Query Planner，
不全量重建知识库，不修改前端，不进入 S4，不处理检索性能重构。

## 2026-07-25 隔离验证

在 NAS API 容器的 `/tmp` 隔离副本中，使用新代码连接当前真实知识数据库，
未覆盖 `/app` 活代码，未修改知识索引：

- S3 无模型合同：12/12；
- S1 失败语义回归：5/5；
- S2 按需能力回归：4/4；
- 旧 RAG Evidence 合同：通过；
- 固定 12 题：Keyword Hit@5 7/8、Vector Hit@5 6/8、
  Hybrid Hit@5 8/8、Hybrid Top-3 8/8、无答案拒答 4/4；
- 8 份受影响演示文档按新切片器预演后，结构型 Chunk 均为 0。

以上是隔离验证，不代表 NAS 已部署。NAS Git HEAD 仍为 `2d1277e`，S1/S2
运行文件相对该旧 HEAD 为 dirty；在未获得针对该实际 dirty 集合的收敛授权前，
不覆盖活代码、不重建索引、不发布 RuntimeRelease，也不报告真实 SSE 已通过。
