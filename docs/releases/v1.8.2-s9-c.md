# V1.8.2-S9-C：Provider多轮请求全量落账与Skill重复调用治理

## 结果

- 成本统计的最小单位收敛为Provider请求；同一Agent阶段内的多轮Provider响应分别保存，不再由最终响应覆盖前一轮Usage。
- 优先使用Provider request_id去重；Provider未返回request_id时使用运行内稳定序号，证据继续保存在现有`model_calls.usage_normalized`中，未新增表或字段。
- 已确定性预加载的Skill继续保留版本、Snapshot、哈希和原文Evidence，但不再向模型重复暴露同一个Skill访问工具。
- 成本页面把业务阶段与Provider请求分开表达，并直接按真实模型ID区分Flash和Pro；无法识别时显示“模型身份待确认”。
- S9-B.2的六种日期范围、服务端分页和三段式页面结构保持不变。

## 历史证据补录

- 仅允许补录Trace `3c1aad38350c4600`和`cedc795859fc458c`中各一条缺失的垂直Agent Provider请求。
- 补录来源限定为Agno Session Run中已持久化的Provider response request_id、实际响应模型和原始三类Usage；脚本对Trace、Session、Run、request_id和Usage使用硬断言并保证幂等。
- 不使用供应商日汇总反推单次请求，不调用模型，不修改聊天、Skill、Badcase或其他历史记录。

## 验证

- 无模型确定性测试覆盖多轮请求不覆盖、request_id去重、缺失request_id稳定序号、逐请求Token与费用聚合、同阶段多请求、Skill工具隐藏及Flash/Pro真实模型ID判断。
- S5、S9-B/S9-B.1和S9-B.2成本合同回归通过，前端JavaScript语法通过。
- 历史补录完成：2026-07-28共6次Provider请求，全部为Flash；缓存命中6,144、缓存未命中7,891、输出903、总Token 14,938，真实成本¥0.00981988，仍归属于2条业务Trace。
- 唯一一次宠物托管Flash验收使用Trace `cc888c3c1fbb434f`：Skill #17真实激活，MCP、RAG证据、Action、Receipt和新Badcase均为0；但回答阶段触发`ModelPrivateAttr`证据容器类型错误，未返回24小时、100元/天和联系电话，因此本次真实验收失败。
- 该确定性根因已改为普通实例级list并新增无模型回归，容器内验证journal为`list`；遵守一次调用上限，未再次请求模型，阶段结论保持PARTIAL。

## 边界

- 未修改价格、RuntimeRelease、Skill业务内容、数据库结构、RAG、目标①②③能力或S9-B.2日期分页逻辑。
- 未调用Pro或Darwin；T+1供应商后台对账待用户次日人工确认。由于唯一一次业务验收失败，本阶段不提前宣称完整PASS。
