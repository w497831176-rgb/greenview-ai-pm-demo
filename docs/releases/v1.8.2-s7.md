# V1.8.2-S7 — Evaluation 失败闭环与 Badcase 工作台

状态：PARTIAL

## 最终结论

S7 的工作台工程、服务端分页、详情可读性、Badcase 与单用例 Evaluation 关联、人工配置证据和 RuntimeRelease 发布均已完成并部署。Badcase #677 的原始路由错误已修复：唯一一次复测实际选择 `customer_service`，不再误选 `mars-greenhouse-agent`。

但唯一获准的 Evaluation Case #2 复测仍为 failed：问题文本中的“预约”触发了 `calendar-server/get_current_datetime`，违反该用例“Tool skipped、MCP 0 次”的硬断言。按调用上限不重试、不继续改配置，因此 #677 保持 fixing，未执行 verify/close，S7 不能标记 PASS。

## 原始失败

- Evaluation Case：#2 / `S6-GOLDEN-002`。
- 固定问题：`请问小区是否提供免费搬家服务？请告诉我专用预约号码、服务时段、免费额度和办理步骤。请只使用知识库直接证据；没有证据就明确说依据不足，不要猜测，也不要转人工。`
- 原 Evaluation Run：#2，failed。
- 原 Trace：`1098e350a28e4e11`，仍为 complete，实际 Agent 仍保留为 `mars-greenhouse-agent`，未被改写。
- 自动 Badcase：#677。
- 原失败中无证据拒答、Citation 0、MCP 0、业务写入 0、Handoff skipped 均通过；唯一硬失败是实际 Agent 与预期 `customer_service` 不一致。
- 确定性根因：Agent #74 的 description 与 instructions 含有“知识库、直接、证据、不足、猜测、专用”等跨域通用词，参与 Router 路由词窗重叠；仅改 description 不足以消除误路由。

## 工程修改

- `db/property_db.py`：新增 Badcase 服务端分页查询；1 次 COUNT + 1 次有界 SELECT，只返回列表所需轻量字段，无逐条详情 N+1；Evaluation 概览新增每个黄金用例最新结果口径。
- `app/badcases.py`：列表 API 返回 `items/total/page/page_size/total_pages`；新增人工审核后的 Agent 配置应用证据与 RuntimeRelease 关联证据动作。
- `app/evaluations.py`：单用例运行可显式关联正在 verifying 的 Badcase；复测成功或失败均回写同一 Badcase，失败返回 fixing，不另建重复 Badcase，原 Run 不改写。
- `frontend/index.html`：Badcase 默认 20 条分页、可选 50 条；列表不预取详情；详情按“问题概览 → 根因 → 方案与人工决策 → 验证发布 → 生命周期”展示，技术 JSON/Darwin/动作原文默认收起。
- `scripts/test_v182_s7_badcase_workbench.py`：隔离临时 SQLite 与模拟运行时的无模型合同测试。
- 未新增数据库表或字段，未修改目标①②、S5 成本底座、ActionGateway、RAG、MCP 或 RuntimeRelease 编排。

## 性能结果

修改前 NAS 内部 `/api/badcases` 连续三次为 284.84 ms、247.14 ms、262.12 ms；每次 469 条、1,506,607 bytes，并包含完整长字段。

部署后默认 20 条分页响应连续三次为 21.17 ms、18.10 ms、21.05 ms，均为 23,345 bytes；相较旧响应体下降约 98.45%。用户外网约 6 秒是体验反馈，本记录不以 NAS 内部耗时替代外网体验。

## 确定性验证

- 隔离测试：43 项 PASS。
- 覆盖路由收窄、火星领域保留、绑定不变、Release Diff、新旧会话 Snapshot 固定、分页/筛选/轻量字段、同一 Badcase 关联复测、原失败 Run 不改写、失败回到 fixing、按需详情加载与详情信息层级。
- Python `py_compile`：PASS。
- 前端内联 JavaScript `node --check`：PASS。
- 全部确定性测试不调用模型，不写 NAS 真实业务数据库。

## 唯一一次 Darwin Pro

- HTTP：200；耗时 44.24 秒。
- Trace：`6ed1a63a8fc446a9`，顶层 complete。
- model_call：#956，阶段 `darwin`，子级 success。
- requested_model / provider_response_model：均为 `deepseek-v4-pro`；Thinking 为 true，但与模型选择分开记录。
- Provider Usage：输入命中缓存 0、输入未命中缓存 602、输出 1,974；`usage_source=provider_actual`；总 Token 2,576。
- 价格快照：Pro 未命中输入 ¥3/1M、命中输入 ¥0.025/1M、输出 ¥6/1M；费用 `(602×3 + 0×0.025 + 1974×6) / 1,000,000 = ¥0.01365`。
- Evidence Ledger HTTP 200；model_call、成本和 Badcase #677/草稿 #18 关联存在；Action Proposal、Receipt、MCP、ActionGateway、Handoff、工单均为 0。
- Darwin 草稿 #18 把失败误判为“安全拒答仍建议转人工”。人工复核依据原 Evaluation 硬断言拒绝该草稿；状态 rejected，未应用、未发布。

## 人工修复与 RuntimeRelease

- 人工采用确定性路由分析，只收窄 Agent #74 的 description 与 instructions；未采用 Darwin 草稿。
- Agent 名称、启用状态与模型覆盖未变；Skill 117、知识文档 124、`mars-calculator-mcp` 绑定未变。
- 发布预览发现 2026-07-26 旧契约测试遗留的两个测试 Agent 和客服 Skill 草稿差异。为避免删除历史测试数据或将其误发布，从当前 v19 不可变快照派生 scoped draft。
- RuntimeRelease：`rr_0025_013cbdee / v25`，parent=`rr_0019_8b4924eb / v19`。
- v19→v25 diff：仅 1 个 Agent modified，仅 `mars-greenhouse-agent.description` 与 `mars-greenhouse-agent.instructions` 两个字段；Agent/Skill/知识/MCP/Policy 无新增、删除或绑定变化。
- v25 已 published，对新会话生效；旧会话继续固定原 Snapshot。

## 唯一一次 Case #2 Flash 复测

- HTTP：200；耗时 47.80 秒；未重试。
- Evaluation Run：#6，failed；Badcase #677 关联存在，状态 verifying → fixing。
- Trace：`14bdba02ae894b7d`，顶层 complete；Session：`evaluation-S6-GOLDEN-002-3aed2371`，固定 `rr_0025_013cbdee / v25`。
- model_call：#957，仅 1 次 Router 模型调用；requested_model / provider_response_model 均为 `deepseek-v4-flash`，`provider_actual`。
- Flash Usage：输入命中缓存 768、输入未命中缓存 1,495、输出 187，总 Token 2,450；费用 ¥0.00188436。
- 路由断言：PASS，expected/actual 均为 `customer_service`。
- RAG：PASS，`rejected_insufficient`；Citation 0。
- Handoff：PASS，状态 skipped，原因 `negated_by_user`；无真实人工接管。
- ActionGateway、工单、草稿、Proposal、Receipt、业务写入：均为 0。
- 失败断言：Tool 预期 skipped、实际 selected；MCP 预期 0、实际 1。
- 唯一 MCP：`calendar-server/get_current_datetime`，只读、success、无 Receipt；匹配原因明确记录为 `触发词=预约`。
- 回答虽然满足当前 required/forbidden terms，但仍出现“或选择转人工核实”，与用户自然语言要求不一致；当前硬规则只验证真实 Handoff 状态，未把该措辞判为失败。这是独立的可读性/规则覆盖限制，不冒充已修复。
- Evaluation Run 与 Badcase 通过 `run.badcase_id=677`、`badcase.linked_evaluation_run_id=6`、`badcase.retest_trace_id` 双向关联；本次聊天 Evidence Ledger 已存在，但其中 `badcase_links` 仍为空，属于后续需要补齐的证据交叉引用限制。

## 当前评估口径与状态

- 黄金用例：5 个。
- 累计历史：4/6 PASS，66.67%。
- 每个用例最新结果：4/5 PASS，80%。
- #677：fixing；未 verify、未 release-observation、未 close。
- #678：仍为 pending，未处理。
- 原 Run #2 与 Trace `1098e350a28e4e11` 保持原貌。

## 模型与副作用边界

- 真实模型调用严格只有：1 次 Darwin Pro 逻辑调用；1 次 Evaluation Case #2 逻辑运行（内部仅 1 次 Flash Router 调用）。
- 未重试，未运行其他 Evaluation Case、Canary、A/B、AI 分类或普通聊天。
- 未创建工单，未调用 ActionGateway，未产生 Proposal/Receipt 或业务写入。
- 未重建 DB、RAG、Web 以外的服务；未修改或提交 `.env`。
- S7 在 PARTIAL 停止，不进入下一阶段。
