# V1.8.2-S6：反馈入口与最小黄金评估集

## 结论

状态：实施中（确定性合同已通过，真实黄金集验收待部署后执行）

本阶段只服务目标③的第一段闭环：把业主反馈与五条关键能力路径沉淀为可重复、可追溯的产品证据。S5 成本底座和目标①②运行时保持冻结。

## 基线

- GitHub main / NAS 运行代码：`90bd02721addf2679e8948a09ba6b0177dc99b75`
- RuntimeRelease：`rr_0019_8b4924eb / v19`
- 数据库：沿用现有 SQLite；仅新增最小 `chat_feedback` 事实表，不新增平台型服务或通用评估框架
- 模型边界：正式验收只使用 Flash；不调用 Pro、Darwin、A/B 或写操作链路

## 用户可见能力

### 1. 反馈入口

- 👍 可不填写原因，保存为正向反馈，不创建 Badcase。
- 👎 原因可选；未填写时保存明确默认值“未提供具体原因”。
- 负向反馈关联原始会话、用户消息、AI 回答、回答消息和 Trace，并创建或复用唯一 Badcase。
- 同一回答重复提交相同反馈返回幂等结果；不会重复创建 Badcase。
- 前端原因统一为：答非所问、事实错误、无依据编造、未解决问题、其他。
- 提交成功后消息卡显示“已点赞”或“已反馈 · Badcase #ID”，并禁用重复点击。

### 2. Golden Set

- 沿用现有 Evaluation Case / Run、Trace、RunEvidenceLedger、model_calls 和 Badcase。
- 用例可以确定性检查 Agent、Skill、RAG、Tool/MCP、Citation、Handoff、模型调用次数和业务副作用。
- 每条断言保存预期、实际、PASS/FAIL 与失败说明。
- 失败运行自动且幂等地创建 `source=evaluation` Badcase，关联 case、run、Trace、回答、失败断言和运行证据。
- Provider 或运行时失败只能得到 FAIL，不会保存为 PASS。
- 受控失败探针明确标记，并从黄金集用例数、运行数、通过率和成本统计中排除。

## 确定性验收

命令：

```text
python scripts/test_v182_s6_feedback_golden_set.py
```

结果：

```text
PASS: V1.8.2-S6 deterministic feedback and Golden Set contract (14 checks)
```

覆盖：

1. 点赞无原因成功；
2. 点赞不创建 Badcase；
3. 点踩保存所选原因；
4. 点踩缺省原因明确；
5. message/session/Trace/问答关联；
6. 重复点踩不重复创建；
7. Evaluation PASS 不创建 Badcase；
8. Evaluation FAIL 自动创建一个 Badcase；
9. 同一失败重复持久化不重复；
10. Badcase 保存 case/run/trace/失败断言/回答/证据；
11. 五类黄金路径的确定性映射；
12. 受控失败探针不计入黄金统计；
13. API 断言包含 expected/actual/failure；
14. Provider 失败不能成为 PASS。

另已通过 Python 语法检查和前端 JavaScript 语法检查。

## 正式黄金集与真实验收

待部署后通过产品 API 创建并各运行一次，结果将补充在本节：

1. `S6黄金集-维修时效有依据回答`
2. `S6黄金集-无依据搬家服务安全拒答`
3. `S6黄金集-精确工单只读查询`
4. `S6黄金集-业主主动要求人工`
5. `S6黄金集-安全风险覆盖不转人工`

若五条全部通过，再运行一次 `S6验收用故障注入-不计入黄金集`；若任一正式用例失败，则使用该真实失败证明 Badcase 闭环，不创建受控失败探针。

## 不做

- 不进入 S7，不调用 Darwin/Pro，不自动归因或自动修复。
- 不建设企业级评估平台、LLM-as-a-Judge、队列、自动调度或供应商对账。
- 不修改 S5 成本底座、ActionGateway、RAG/MCP 编排或 RuntimeRelease。
- 不使用 Playwright、浏览器自动化或浏览器容器。
