# V1.8.2-S4-A.1：历史 Release 差异可视化修正

状态：代码、测试、Git、NAS Web-only 部署与性能回归已完成；人工 UI 体验待用户验收  
复杂度：S  
范围：历史 RuntimeRelease Diff 的前端信息层级与可读性

## 用户可见问题

S4-A 已经提供确定性历史 Diff，但详情放在约 448px 的全局 Drawer 中。桌面两列
对比在该宽度内仍会退化为上下排列；修改前、修改后缺少固定列标题；历史入口只写
“查看变更”，用户必须自行判断比较对象。统计卡片和工程配置还会先于 Agent 主线
占据注意力。

## 最小修正

- 历史 Diff 改为独立 `max-w-6xl` 大尺寸 Modal；完整原始快照继续使用 Drawer；
- 标题固定展示 `Release #父版本 → Release #当前版本`；
- 同时展示两个 Release ID、发布时间和当前运行标识；
- 根据真实 Diff 在前端确定性拼装一句话变更结论，不调用模型；
- Agent 作为第一层主卡片，字段使用“变更项 / 修改前 / 修改后”三列表格；
- Prompt 默认只展示前三行与有限长度，点击后展开完整文本；
- Skill、RAG、MCP 挂载明确拆为“新增能力”和“移除能力”；
- 其他工程配置移动到 Agent 之后，并默认折叠；
- 重复快照、初始版本、新增对象和删除对象都有明确占位状态；
- 历史 Diff 仍只在点击时请求，不改变 Overview 首屏接口。

## 明确未改

没有修改 RuntimeRelease 数据结构、Diff 算法、API、数据库、平台配置、历史数据、
Agent/Skill/RAG/MCP、S1/S2/S3 或 S4 动态能力；没有调用真实模型或使用浏览器
自动化。

## 测试与真实历史兼容性

- S4-A.1 前端合同：10/10；
- S4-A 原发布中心合同：7/7；
- Node 20 前端语法检查：通过；
- 真实 Release #12 → #16 包含两个 Agent 的 Prompt、Skill、RAG 与 MCP 变化，
  现有接口字段可直接支持新版展示；
- Release #13、#14 是真实重复快照，Release #1 是真实初始版本；
- 未使用 Playwright、浏览器自动化或真实模型。

## 性能回归

| 指标 | 部署前 | 部署后 |
|---|---:|---:|
| Overview 响应体 | 9,538 bytes | 9,538 bytes |
| NAS 三次耗时 | 0.145～0.154 秒 | 0.133～0.144 秒 |
| 首屏历史 Diff 请求 | 不请求 | 不请求 |

历史 Diff 仍只在用户点击“与 #父版本比较”后加载。

## Git 与 NAS

- 功能分支：`agent/v1.8.2-s4a1-history-diff-ui`；
- 功能提交：`ea311087473e3f8d189799b9c5dc91e3c85eee8e`；
- PR：`#4`；
- 合入 `main`：`15214ade9aef4ecf5bc658031f0a671d2406c067`；
- 只重建 `demo-os-web`，新容器 ID：`f5c830e79fb9`；
- `demo-os-api` 保持 `e0910886f7a2`，`demo-os-db` 保持 `7c292145a579`；
- RuntimeRelease 保持 `rr_0018_74d4c896` / version 18 / `published`；
- RuntimeRelease 总数保持 18；
- NAS dirty 仍仅为未暂存的 `.env`，本次未读取、覆盖或提交；
- 部署前备份：`/tmp/yiai-s4a1-predeploy-20260725.tar`；
- 备份 SHA256：
  `e242a5e4a093767eac971754778af6af136bd02f15af47b1e0ed209105186951`。

最终视觉感受仍由用户手点验收，工程合同和接口证据不能替代人工 UI 体验。
