# V1.8.2-S9-B.1：调用与成本治理页面极简化

## 结果

- 默认页面收敛为“今天一句话结论、今日调用记录、成本治理原则”三个区域。
- 高成本排行与调用列表合并；工程筛选、价格、预算和策略说明默认折叠。
- 左侧仅保留“调用与成本治理”，不再提供独立“成本优化策略”入口。
- 调用详情按“发生了什么、为什么选择、花费来源、下一步建议”展示，Token拆分与计算明细默认折叠。
- 无模型调用明确显示费用不适用；Usage证据缺失明确显示金额无法计算，不按0元处理。

## 边界

- 仅修改前端信息层级与确定性合同测试；成本API、数据库、价格、模型路由和历史数据均未修改。
- 未调用任何模型，未发布RuntimeRelease；仍使用 `rr_0026_a27a63e8 / v26`。
- 页面视觉效果由用户手动验收，未使用浏览器自动化。
